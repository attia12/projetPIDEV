{{ form_start(form) }}

<div class="form-group">
    {{ form_label(form.content) }}
    {{ form_widget(form.content) }}
</div>
<div class="form-group">
    {{ form_label(form.author) }}
    {{ form_widget(form.author) }}
</div>
<div class="form-group">
    {{ form_label(form.id_article) }}
    {{ form_widget(form.id_article) }}
</div>

<button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Save commentaire') }}</button>
{{ form_end(form) }}